# Tasks: Match Surfaces At A Glance Hierarchy

- [x] Compare local `/matches` and individual match hierarchy against CREX, Cricbuzz, and ESPN Cricinfo.
- [x] Add Spec 028 docs for the matches-page and individual-page at-a-glance cleanup.
- [x] Upgrade `/matches` summary cards into a true at-a-glance layer.
- [x] Move `/matches` discovery-heavy sections below the visible match list.
- [x] Keep `/matches` crawlable links in SSR HTML while making their headings more user-facing.
- [x] Make the top individual match details card explicitly read as the at-a-glance layer.
- [x] Tone down the secondary SEO summary block on the individual page without removing it.
- [x] Rebuild the frontend and verify `/matches` plus a sample `/cric-live/{slug}` route locally.
