# Canonical Live-Match Ranking Tasks

Status legend: `[ ]` queued, `[-]` in progress, `[x]` verified, `[!]` blocked.  
Rule: do not mark a task complete on source edits alone. Record the requested evidence in the task before moving forward.

## Current focus

**Current active tasks: R1.1, R1.3, R1.4, R2.5, and R3.1 — complete the remaining upcoming cohort contract, maintain live/completed evidence as the catalogue rotates, then prove analytics receipt and GSC outcomes.** R0.6 and R4.2 are live and verified. Production frontend release `20260809-prematch-ssr-r1-63f00d1` is now live and healthy; its SSR prematch lane exposes the sampled upcoming URLs from `/live-score`, `/live-score/today`, and `/cricket-schedule/today`. The first post-rollout cohort is 8/9 technically ready (88.9%); one sparse Tezpur–Jorhat page lacks a valid `SportsEvent` location and remains blocked rather than receiving invented schema. The standalone StartupOS monitor now persists the same canonical cohort across runs and calculates timed T−1h GSC outcomes. Do not claim indexing or ranking improvement until the three-lifecycle cohort and outcome gates pass.

## Spec 051 — crawl receipt and index selection (2026-08-13)

**Decision:** Treat Google crawl receipt and index selection as separate problems from technical readiness. Keep valid SportsEvent markup, repair venue/location and deterministic SSR reliability at the data boundary, select a fixed upcoming cohort, and measure each canonical URL at T−48/T−24, T−6, T−1, start, and T+24–72. Do not use the Google Indexing API for ordinary SportsEvent pages; manual URL Inspection is limited to one or two technically valid priority URLs per cohort.

**Implemented locally:**

- Spec Kit feature `specs/051-crawl-receipt-index-selection/` now contains the requirements, implementation plan, evidence model, tasks, and verification quickstart.
- The CREX schedule parser now carries venue from trusted schedule JSON-LD or explicit ground/venue elements, normalizes whitespace, and rejects `null`, `TBD`, `N/A`, `unknown`, and equivalent placeholders before they reach schema.
- Upcoming backend catalogue rows now reuse the existing canonical snapshot enrichment path when venue/team/state identity is missing; no source URL or guessed location is changed.
- Frontend structured data keeps the existing `SportsEvent` startDate+location guard. `NewsArticle` is now gated behind the existing high-value live-coverage rule, at least three substantive update bodies, a non-upcoming lifecycle, and a real modification timestamp. Upcoming score pages remain on the normal `Article` contract; there is no blanket NewsArticle migration.
- The fixed cohort and GSC evidence model remains in the standalone StartupOS ledger and is explicitly carried into Spec 051. Missing GSC observations remain pending; HTTP 200, sitemap membership, schema validity, and manual queue acceptance are not indexing evidence.

**Verification:** `python -m pytest tests/unit/test_crex_schedule_parser.py -q` passed (9 tests). `mvn -q -DskipTests compile` passed. The Angular browser build passed with `NODE_OPTIONS=--openssl-legacy-provider`; the default local Node/OpenSSL invocation is blocked by the known webpack `error:0308010C` compatibility error. The full Angular Karma run could not be completed in this environment and is still a release gate. No production deployment was made in this slice.

**Next gates:** add focused backend/SSR assertions for venue hydration and NewsArticle eligibility, run the existing production rollout guard, re-enable the paused Windows cohort monitor before the next cohort, and collect explicit GSC Last crawl/coverage observations. The business target remains unproven until at least three clean cohorts reach the T−1h indexed-by-Google threshold.

## Prematch discovery cohort-links experiment (2026-08-09)

**Decision:** Create and link the canonical `/cric-live/{slug}` page 12–48 hours before start. The same URL remains the lifecycle owner for upcoming → live → completed. An opening-model row is required before publishing a win prediction, but it is **not** a prerequisite for early crawl discovery.

**Primary outcome KPI:** Google Search Console discovery and indexing. The technical score below is only a prerequisite for giving Google a clean crawl path; it is not GSC discovery or indexing and must never be reported as either.

**90% operating target (measured, not guaranteed):**

- **Technical discoverability gate:** at least 90% of sampled upcoming URLs must return 200, self-canonical, `index,follow`, match-specific title/H1 and scheduled context, valid `SportsEvent`, sitemap membership, and at least one server-rendered link from the home/matches/live-score/today/schedule hub set.
- **Google discovery gate (primary):** at least 90% of valid samples with explicit GSC URL Inspection observations must be `Discovered` or `Indexed` within 24 hours of first sitemap publication. Missing/403/unknown inspection data stays `pending` and is never counted as success.
- **Google indexing gate (primary, delayed):** indexed status is measured separately at T+24–72h and only from explicit GSC `Indexed`/`URL is on Google` evidence. The technical score, sitemap membership, live tests, or rankings do not count; a 90% indexing rate is not claimed until this cohort supplies the denominator and outcome data.

**Pre-live outcome target (user goal):** for at least 90% of valid cohort URLs, an explicit GSC observation captured no later than **one hour before scheduled start (T−1h)** must show both Google discovery (`Discovered` or `Indexed`) and Google indexing (`Indexed`/`URL is on Google`). A page that is only `Discovered – currently not indexed` passes discovery but fails the pre-live indexing goal. This is a measured go/no-go target, not a guarantee Google will meet on every cohort.

**Sampling schedule:** T−48h/T−24h (cohort selection and page contract), T−6h (hub/sitemap recheck), **T−1h (GSC pre-live discovery/index decision)**, match start (same canonical URL becomes live), T+6h (lifecycle handoff), and T+24–72h (post-live GSC follow-up). The denominator is the set of valid exact upcoming candidates selected in the window; no model row is fabricated for a missing opening artifact. The monitor must retain cohort identity across these follow-ups instead of treating each run as a new 12–48-hour sample.

**Standalone monitor:** `C:\Users\ADMINS\Documents\projects\startupOS-open-ended-thread-os` now contains the `seo` StartupOS agent and the recurring task `seo-crickzen-prematch-cohort-001`. It writes `logs/seo/prematch-discovery/cohort.json` and `cohort.md`, plus the persistent `cohort-ledger.json` and append-only `cohort-history.jsonl` keyed by canonical URL. It runs without Codex/OpenCode, can be directed by the OpenCode Go agent at `.opencode/agents/crickzen-prematch-discovery.md`, and is now invoked by the Windows task `Crickzen Prematch GSC Cohort Monitor` every 30 minutes with missed-run recovery and overlap protection.

**Baseline run (2026-08-09T11:23Z):** 9 of 33 upcoming rows were in the 12–48-hour window. All 9 were 200/self-canonical/indexable and in `sitemap-matches-0001.xml`; the run found no explicit GSC observations, so Google discovery/indexing gates remain pending. The current SSR hub responses contained no exact links for these sampled upcoming slugs, making the technical cohort rate 0% until the pre-match SSR link lane is deployed. This is the experiment's first controlled baseline, not an indexing claim.

**Persistent-ledger implementation (2026-08-09T13:05Z):** StartupOS now records `firstSeenAt`, `sitemapFirstSeenAt`, `ssrLinkFirstSeenAt`, scheduled start, explicit GSC observation history, first discovered/indexed timestamps, sitemap-to-GSC latency, and the T−1h pre-live margin for each canonical URL. Three post-rollout runs have retained the same 9 match identities; no timed GSC observations have been supplied, so `preLiveGscGate=pending_gsc_timed_evidence`. The follow-up path also accepts observations for URLs that have left the upcoming window and merges them into the existing identity. The artifact paths are `logs/seo/prematch-discovery/cohort-ledger.json` and `cohort-history.jsonl` in the StartupOS harness. Technical readiness remains 8/9 (88.9%) because one page lacks reliable venue/location for valid `SportsEvent` schema.

**Windows scheduler checkpoint (2026-08-09T13:31Z):** The task `Crickzen Prematch GSC Cohort Monitor` is enabled and runs `scripts/run_crickzen_prematch_seo.ps1` as the interactive ADMINS user every 30 minutes. `StartWhenAvailable` is enabled, overlapping runs are ignored, and the execution limit is 10 minutes. The first scheduled run completed with result `0` at `2026-08-09T19:00:47+05:30`; the cohort artifact and ledger were updated at `2026-08-09T19:01:07+05:30`, with 9 samples and `preLiveGscGate=pending_gsc_timed_evidence`. The next run is scheduled for `2026-08-09T19:30:47+05:30`. The legacy daily `Crickzen SEO Audit Agent` remains unchanged until this harness task has a stable run history.

  Guard robustness checkpoint (2026-08-01T19:38Z): an unmodelled ARG-W/CAN-W fixture rotated to the head of the completed catalogue and made the prior first-supported selection look like a retention failure. The retained-model resolver still returns valid completed rows (for example BP/WF); the defect was monitor selection, not production SSR or retention. `Assert-LiveMatchCohortReadiness.ps1` now prefers an exact source-resolvable completed row with a final probability and at least two history points, while preserving first-supported fallback if no retained row exists. Re-run: live LS/SB and completed DW/TT are eligible; upcoming ARG-W/CAN-W remains correctly blocked.

  Live-window checkpoint (2026-08-02T19:52Z): the production live catalogue contained only PAK/WI Test (`pak-vs-wi-2nd-test-pakistan-tour-of-west-indies-2026-match-updates-11B5`). Tests are outside the controlled T20/Hundred/ODI cohort, so the readiness guard correctly reports `NO_SUPPORTED_CANDIDATE` rather than a model, freshness, or SSR regression. Keep the live gate pending the next supported live fixture; completed remains eligible and upcoming remains blocked by the absent exact opening row.

  Live-window recovery (2026-08-03T07:47Z): the guard selected `ap-vs-bt-7th-match-asian-legends-league-t20-2026-match-updates-134V` as `LIVE`. Its exact public-model row is fresh, lifecycle-ready, and SSR-visible on a 200/self-canonical/indexable/one-H1 page with no stale upcoming score, so the live lifecycle gate is eligible again. This is production contract evidence only; upcoming still has no exact opening row and this does not establish Google crawl, indexing, ranking, or engagement.

  Freshness recovery (2026-08-03T08:54Z): the scraper remained healthy and selected PD/WDL, AP/BT, and CS/TT, but dashboard state files and public model rows had stopped advancing at 08:52 while the dashboard's HTTP health still returned 200. A scoped `crickzen-dashboard` restart on the unchanged `machine_learning_bbl-dashboard:20260801-opening-r1` image restored new state-file writes and exact public rows (PD/WDL `97/1`, 10.0 overs, 83%, updated `08:54:04Z`; AP/BT and CS/TT also refreshed). The readiness guard then again marks live PD/WDL eligible. This proves the writer resumed; it does not prove Google crawl, indexing, ranking, or engagement.

  Catalogue-identity release checkpoint (2026-08-03): production still retains malformed partial CREX records such as `vs-bt-...-134V` alongside the valid `ap-vs-bt-...-134V` fixture. This makes the readiness guard select an invalid upcoming page and incorrectly frame the absence of an opening row as the gate. The pending backend repair validates CREX score URLs, normalizes records to the stable trailing CREX API key, converts literal `"null"` fields to null, and soft-deletes duplicate rows while keeping the usable identity. `CrexMatchUrlHelperTest` and `LiveMatchServiceImplTest` pass locally. It is release-ready but not yet deployed; after a narrow backend rollout, re-run the readiness guard and require an exact valid 12–48-hour candidate before evaluating opening coverage.

  Live handoff recovery and guard bound (2026-08-09): the scraper selected five current live URLs, while the public feed repeatedly refreshed only a completed MSG/SB state. The dashboard state file for MSG/SB was being rewritten despite the stale lifecycle identity, so a scoped restart of `crickzen-dashboard` on unchanged image `machine_learning_bbl-dashboard:20260801-opening-r1` was justified. Within 18 seconds the public feed emitted exact current rows for SRL-W/WF-W, SCO/UAE, CSG/DD, and the live NDS/NDT fixture; NDS/NDT later verified as a fresh exact `81%` T20 all-gender v2 row updated `2026-08-09T10:58:28Z`. The readiness guard passes live NDS/NDT and retained completed MSG/SB; upcoming DBS-W/LEI-W still has no opening row. The guard was also bounded to eight completed resolver probes with a two-second per-probe timeout so the 2,000+ row completed catalogue cannot hang monitoring. This proves handoff recovery and SSR contract only, not Google crawl, indexing, ranking, or engagement.

## R0 — Baseline and decisions

- [x] **R0.1** Create India/English OpenSEO research project and connect GSC.  
  Evidence: `Crickzen India SEO` project `222ae85a-48fd-451b-956a-8ccb20c57fb4`; GSC reads 38 rows for 2026-06-30→2026-07-28.
- [x] **R0.2** Define P1 demand clusters and prohibited language.  
  Evidence: `docs/seo/keywords/intent-backlog-2026-07.md`.
- [x] **R0.3** Freeze one-canonical-page-per-match policy.  
  Evidence: Spec 044 and `docs/seo/canonical-live-match-ranking-program.md`.
- [x] **R0.4** Inspect the current production live catalogue in GSC and verify raw page eligibility.  
  Evidence: 2026-07-31 sample of five live URLs: four `Discovered - currently not indexed`, one `URL is unknown to Google`; two raw samples were 200/self-canonical/indexable with one H1 and JSON-LD, but lacked win-probability language in SSR. Production scheduler is healthy but had used its 180-URL daily budget. No manual per-URL submission is approved today.
- [x] **R0.5** Verify scheduler, sitemap, and Googlebot parity.  
  Evidence: production status reports GSC/sitemap scheduler healthy; all five sampled URLs are in the live sitemap shard with current `lastmod`, `hourly`, and `0.9` priority; focused Maven suite passed 20 tests. Googlebot receives a 200, self-canonical, `index,follow`, one-H1 `SportsEvent` page. It still lacks SSR win-probability content.
- [x] **R0.6** Correct the discovery transport contract before treating automatic URL requests as indexing.  
  Reason: Google restricts the Indexing API to JobPosting or genuine livestream pages with `BroadcastEvent` inside `VideoObject`; the sampled Crickzen page contains `SportsEvent` only. The scheduler's former "Indexed" counter meant a notification was accepted, not that Google indexed the URL.  
  Local implementation (2026-08-01): `LiveMatchIndexingScheduler` defaults `gsc.live-match-indexing.indexing-api-notifications-enabled` to `false`; controller/status terms now report notifications rather than indexed URLs; the manual trigger explains the standard sitemap + SSR-link + URL-Inspection path. Focused Maven suite passed 21 tests, including the disabled-notification guard.  
  Production proof (2026-08-01): backend image `macubex/victoryline-backend:20260801-seo-discovery-r0-6` (registry digest `sha256:c5751290fe3a7c18657acc7e5e82cadda4acef52420af0ddc6a64d2775c21db4`) is healthy. The public status endpoint reports `indexingApiNotificationsEnabled=false`, `notificationCount=0`, and standard discovery as sitemap + crawlable SSR links + URL Inspection. The sitemap scheduler remains healthy. This proves the operational counter no longer claims Google indexing.

## R1 — Canonical match-page intelligence

- [-] **R1.1** Select 3–9 controlled samples: at least one upcoming, live, and completed match with stable match identity and model eligibility.  
  Depends on: R0.1–R0.3.  
  Current evidence (2026-08-01): the dashboard handoff was recovered after a stale predictor tree grew to 14,253 PIDs. The predictor group-termination guard is now deployed in dashboard image `macubex/crickzen-dashboard:20260801-predictor-cleanup-0425234`; the replacement is healthy at 13 PIDs and the prior image is retained as `machine_learning_bbl-dashboard:rollback-20260801-0202`. The guard source is committed as model-repo commit `0425234` and its focused suite passed 19 tests. This is an availability safeguard, not cohort proof.  
  Latest readiness run (2026-08-01, live window): scraper selected the exact live EDR/SDS source URL and the public feed resolved it with a current T20 all-gender v2 probability/history row. The readiness guard originally misread its timezone-less UTC timestamp as IST; `213d973` now interprets that legacy form as UTC. The canonical live SSR handoff then exposed a stale-precedence defect: a numeric score-feed `lastUpdated` masked the fresh model `updated_at`; `41423f9` now prefers the model timestamp and supports numeric epochs. Production frontend image `macubex/victoryline-frontend:20260801-live-intelligence-ssr-r4` (image `sha256:cb93b5c4cd9504c29d0ec9aa55d3bbe6d2e3bc34fa5bd7f277aff6e2423cf265`) is healthy. Direct frontend SSR plus normal and Googlebot public HTML all visibly show `Live match intelligence`, EDR's live 69% win probability, model label, and updated timestamp; the readiness guard marks this live row eligible. Completed `msg-vs-tr-14th-match-the-hundred-2026-men-match-updates-ZKN` remains eligible.  
  Repeatable verification: `scripts/Assert-LiveMatchCohortReadiness.ps1` checks the production upcoming/live/completed catalogues, exact `match_url` model identity, five-minute freshness, and the canonical SSR contract. Its first run rejected all three lifecycle samples for absent exact model rows.  
  Opening-model checkpoint (2026-08-01): model commit `8db1021` runs a prior-only, whole-date temporal holdout with Platt calibration fit only on earlier OOF rows. Its 1,491-row calibrated final holdout improves Brier (`0.2258` versus neutral `0.2500` and historical rate `0.2444`) and log loss, but female ECE is `0.0705` above the written `0.050` gate. Follow-up model commit `b6aa282` restores exact-ID Cricsheet event names for 5,300/5,363 fixtures; current named holdout events are individually small (largest 49), so they remain diagnostic. Decision: **shadow-only revise**; it authorizes no fixture ingress, public model row, or SSR change.  
  Terminal-handoff repair (2026-08-01): the rotation exposed completed EDR/SDS with an exact retained 100% model row and history, but provider status `stopped` caused canonical SSR to apply the live freshness gate and suppress the final answer. Frontend commit `d9b9b16` now trusts an explicit canonical `completed` lifecycle before provider status; it does not classify `stopped` alone as completed. Commit `a56d340` pins the Jasmine type package compatible with TypeScript 3.2 and updates the lifecycle test factory. Both focused lifecycle type-check and local/Node-12 Docker SSR builds passed. Production image `macubex/victoryline-frontend:20260801-terminal-intelligence-r1` (digest `sha256:2dd6b92ee6f3605d5f4e07ef2f5b681ea59c9864f1d5a77530c6359e1a6bc22b`) is healthy. Googlebot raw HTML for EDR/SDS is 200/self-canonical/indexable/one-H1 with `Completed match intelligence`, `100%`, and no stale `Upcoming 0/0`; a 390px hydrated browser retains the bare canonical URL and the same final answer. The readiness guard now marks live BP-W/WF-W and completed EDR/SDS eligible.  
  Opening-ingress checkpoint (2026-08-01): model commits `b4b68fc`, `f7c18a8`, `4c76e24`, and `07a141d` promote the *offline* Elo candidate into a separately named opening candidate/status/serializer path. Scraper commits `1a7ded6` and `3cf69e1` provide a capped `/prematch-candidates` slate of exact, T20-only, 12–48-hour upcoming URLs with source identity metadata; it does not mutate the live slate. The public serializer emits a row only when the source URL, team resolution, TTL, team-history minimum, and as-of date are valid; its 14-day history-age guard returns explicit `not_ready` reasons otherwise. The generated local artifact ends `2026-07-02`; for a current August ARG-W/CAN-W example it returns `opening_history_too_old_for_fixture`, so no public percentage is released. Focused model suites: opening baseline 15 passed; dashboard opening/public 14 passed; scheduler 11 passed.  
  Historical-refresh checkpoint (2026-08-01): Cricsheet's updated T20 archive added 67 clean new JSON records (5,591 total; none changed/removed). Hash-based incremental ingestion completed with zero errors and extends raw history through 2026-07-30. A new versioned, local-only Elo artifact has 109 team states and 5,001 calibration rows, so the former historical-date gap is resolved locally. It is deliberately not deployed: the current three scraper-selected 12–48-hour fixtures all return `team_not_covered_by_artifact`, which is the correct safety result. The focused opening-baseline suite passes 15 tests; dashboard test collection is locally blocked by a missing SQLAlchemy dependency. Next deploy gate is an exact upcoming candidate that is T20, source-resolved, covered, and inside the configured window; only then publish one isolated opening row and perform SSR/mobile/Googlebot proof.
  Latest production readiness (2026-08-01): live BP-W/WF-W and completed BB/CS remain eligible. Upcoming HT-W/MW-W is canonical but has no exact public-model row and no SSR intelligence. The new opening code is not deployed, so this is expected and no production probability claim follows.  
  Retained-source SSR repair (2026-08-01): the rotating completed LS-W/SB-W sample exposed a final identity defect: its exact retained public resolver returned a stopped, 100% Hundred row with history, but canonical SSR omitted it because the rolling feed could contain a same-team live record. Frontend commit `474a005` returns `null` after canonical source-URL mismatch, forcing exact retained-source resolution rather than team fallback. It was built from a clean archive of that pushed commit and deployed as `macubex/victoryline-frontend:20260801-retained-intelligence-r3` (image `sha256:3e26683738761968f48d8569d9f4ec8c126598fbe42dfe5f0fff4474c57b742f`); the prior `20260801-upcoming-fixture-r2` image is tagged as rollback and `.env` was backed up. Normal and Googlebot raw HTML are 200/self-canonical/indexable/one-H1 with visible `Completed match intelligence`, `100%`, and no stale upcoming `0/0`; the readiness guard marks the current completed HT-W/MW-W sample eligible. During the same live window, scraper-selected LS/SB was exactly matched to a fresh 57% public row and normal/Googlebot SSR showed the answer; a later guard correctly rejected it only because its row aged past five minutes. The only product-contract blocker remains upcoming: ARG-W/CAN-W has no exact opening row or SSR intelligence.
  Current opening gate (2026-08-01): the local historical T20 input/artifact refresh is complete through 2026-07-30, but no production deployment is authorized yet. Every current scraper-selected 12–48-hour candidate is outside the artifact's team coverage and safely returns `team_not_covered_by_artifact`. Wait for one exact, source-resolved, covered T20 candidate inside the configured window; then deploy the isolated scraper/dashboard handoff, publish one exact opening-model row, and prove SSR/mobile/Googlebot parity before freezing the three-lifecycle cohort.
  Latest readiness (2026-08-02): exact live CDK/ODW (`cdk-vs-odw-4th-match-delhi-premier-t20-league-2026-match-updates-13B8`) is `LIVE` with the exact CREX source URL, a fresh public 49% row updated `2026-08-02T07:54:34.152176Z`, and visible `Live match intelligence` / win probability in 200, self-canonical, `index,follow`, one-H1 Googlebot HTML. The guard also finds retained completed PAK-W/SL-W eligible. Upcoming ARG-W/CAN-W remains blocked solely by the absent exact public opening-model row and SSR intelligence, so the three-lifecycle cohort is still not ready and no indexing/ranking conclusion follows.
  Latest live rotation (2026-08-02T10:06Z): exact LIVE SRL-W/TR-W (`srl-w-vs-tr-w-17th-match-the-hundred-2026-women-match-updates-ZKQ`) resolves by its CREX URL to `The Hundred all-gender v1`, 47%, updated `2026-08-02T10:06:10.189155`, with 11 history points. The readiness guard marks it fresh/eligible. Googlebot canonical HTML is 200, self-canonical, `index,follow`, one H1, visibly contains `Live match intelligence` and `win probability`, and has no stale `Upcoming 0/0`. This retains the live gate; it does not establish Google crawl, indexing, ranking, or engagement.
  Opening-candidate ingress repair (2026-08-02): production was parsing only CREX's initial schedule date, leaving the visible upcoming catalogue with at most 5.5 hours of lead time and making the 12–48-hour opening window unreachable. Scraper commit `150bbf8` follows one bounded `Next >` schedule page, deduplicates canonical URLs, rejects the observed nested `/cricket-live-score/vs-...` fragment at both parser and retained-candidate boundaries, and has focused discovery/schedule/prematch tests. Image `macubex/victoryline-scraper:20260802-schedule-lookahead-r4` (digest `sha256:3d450bad761f5a79b3164550c5f41d1fa1d793900622d0ec925106a29c101c21`) is healthy. Its public `/prematch-candidates` feed now exposes exact T20 candidates Asian Stars/Bangladesh Tigers, West Delhi Lions/Purani Dilli-6, and Charaideo Sunrisers/Tezpur Titans; no malformed nested fragment remains. This closes schedule ingress only: none has yet passed model-artifact coverage and opening-row publication, so SSR upcoming proof remains blocked.
  Runtime-artifact correction (2026-08-02): the historical refresh had created a July-30 artifact in a dated output folder, while the configured runtime path still held a July-2 copy. Rebuilding `artifacts/opening-baseline/t20_all_elo64_runtime_v1.json` from `data/t20_all_raw/matches` now produces `as_of_date=2026-07-30`, 109 team states, and 5,001 calibration rows. After installing the dashboard's declared requirements into the local virtual environment, focused dashboard opening/public tests pass 14 and the opening-baseline suite passes 15. All three currently public candidates still safely return `team_not_covered_by_artifact`. No dashboard deployment is authorized until an exact covered candidate appears.
  Proof: exact URLs, slugs, lifecycle, model freshness, and test owner recorded in `docs/seo/reports/weekly-query-to-intelligence-report.md`.
  Heartbeat evidence (2026-08-10T10:06Z): production returned 16 upcoming, 5 live, and 2,056 completed rows; the public prediction feed returned 8 rows and both scraper candidate endpoints returned HTTP 504. The guard now rejects an upcoming row whose `scheduledStartTime` is already before the current UTC time, preventing the stale EDR/WDL `UPCOMING` record from masquerading as pre-match readiness. It selects future `dw-vs-gr-20th-match-assam-premier-league-2026-match-updates-13AF`, which has no exact public opening-model row. The live sample `nds-vs-ndt-18th-match-delhi-premier-t20-league-2026-match-updates-13BM` has an exact fresh running T20 all-gender v2 row (`updated_at=2026-08-10T10:06:01Z`, 49%), and its canonical page is 200/self-canonical/indexable, but the raw response is the existing SSR fallback without `Live match intelligence` or a win-probability block. Therefore lifecycle/model freshness passes, while the live SSR intelligence gate is false and needs a narrow frontend SSR handoff intervention. Completed rotation is currently eligible (`bb-vs-bl-17th-match-assam-premier-league-2026-match-updates-13AC`). This is catalogue/model/SSR evidence only; GSC discovery, indexing, and ranking remain unobserved.
  Heartbeat evidence (2026-08-10T10:24Z): catalogue endpoints remained reachable but slow (16 upcoming, 5 live, 2,056 completed). The public prediction feed returned HTTP 502, and both `/scraper/prediction-candidates` (HTTP 502) and `/scraper/prematch-candidates` (HTTP 502/45-second timeout) failed during the sweep. The readiness script could not complete because the public model feed timed out; no exact fresh live row can be asserted from this run. This is a production feed/proxy availability gate, not evidence of Google discovery, crawling, indexing, or ranking. No production mutation was made.
  GSC cohort inspection (2026-08-10T11:04Z): URL Inspection was run for the six current 12–48-hour upcoming canonical URLs (`HK/TAN`, `ITA/UGN`, `CDK/NDT`, `CS/GR`, `DUR/WOR`, `KT/WARKS`). All six report **Discovered – currently not indexed**. GSC detects both `sitemap.xml` and `sitemap-matches-0001.xml` for every URL, but reports no referring page and `Last crawl: N/A` for every URL. The StartupOS ledger now records these observations, raw coverage state, sitemap evidence, referring-page nulls, and explicit `lastCrawlAt=null`. Cohort result: technical 6/6 (100%), GSC discovered 6/6 (100%), indexed 0/6 (0%); the discovery gate passes, while indexing and T−1h pre-live indexing remain open. This is the first direct current-cohort Google evidence and does not establish ranking.
  Manual crawl-queue intervention (2026-08-10T12:35–12:43Z): after rechecking the verified cohort in Search Console, the normal UI **Request indexing** action was confirmed for all six URLs. Google’s confirmation says each URL was added to a priority crawl queue; it does not mean crawled or indexed, and repeated submissions do not increase priority. The audit log is `C:\Users\ADMINS\Documents\projects\startupOS-open-ended-thread-os\logs\seo\prematch-discovery\gsc-manual-requests.json`; `apiRequestsMade=false`. Next proof is a fresh inspection showing `Last crawl`, then an explicit indexed state, ideally before each scheduled start.
  Readiness regression (2026-08-10T12:51Z): the exact NDS/NDT public row is still fresh (`updated_at=2026-08-10T12:50:10.918095Z`, 49%), but a repeat canonical probe returned 200/self-canonical/indexable/one-H1 with `X-SSR-Fallback-Level: snapshot` and no visible `Live match intelligence` or win-probability block. The readiness guard therefore regressed to `live=not eligible` on the SSR-intelligence gate while the model/lifecycle gate remains healthy. This is a narrow production SSR handoff instability, not a GSC indexing result; no broad SEO mutation or Indexing API request is justified.
  Heartbeat evidence (2026-08-10T12:53Z): the guard still selects future DW/GR as the upcoming sample with no exact public opening-model row; NDS/NDT still has a fresh exact live row but its SSR intelligence block is absent, so `live=not eligible` remains. Completed BB/BL is now an eligible retained sample: its exact resolved row has final probability 100% (`updated_at=2026-08-09T12:49:41.895026Z`) and five retained history points, and the canonical SSR contract includes the win-probability block. This is a completed-lifecycle gate recovery only; GSC discovery/indexing and ranking remain unchanged and unclaimed.
  Heartbeat evidence (2026-08-10T13:24Z): the public feed contains 10 rows. The selected upcoming DW/GR URL now has an exact public row, but `updated_at` and `win_probability_pct` are null, so the opening artifact is not usable/fresh and `upcoming=not eligible` remains. NDS/NDT has a fresh exact live row (`updated_at=2026-08-10T13:23:05.106574Z`, 49%) but the canonical page still lacks SSR-visible live intelligence, so `live=not eligible` remains. Completed rotation selects MIL/TR with a retained 100% row and 100+ history points, but the current canonical probe lacks the SSR win-probability block, so `completed=not eligible` in this run. These are model/SSR lifecycle gates only; no GSC indexing or ranking claim follows.
  Heartbeat evidence (2026-08-10T12:24Z): the public prediction feed is healthy with 7 rows, and the exact live NDS/NDT source URL resolves to a fresh running T20 all-gender v2 row (`updated_at=2026-08-10T12:24:17.510735Z`, 49%). Its canonical page is 200/self-canonical/indexable with one H1 and now contains SSR-visible `Live match intelligence` plus a win-probability block; the readiness guard therefore marks `live=eligible` and the live SSR gate has recovered. The future DW/GR upcoming row still has no exact opening-model row, so the pre-match model gate remains blocked; completed BRN/ITA remains non-eligible on its canonical SSR contract. This is a production model/SSR gate change only; the direct GSC cohort evidence remains 6/6 discovered and 0/6 indexed, and no ranking claim is made.
  Heartbeat evidence (2026-08-10T10:55Z): the public model feed recovered with HTTP 200 and 8 rows. The live NDS/NDT source URL again resolves to a fresh exact T20 all-gender v2 row (`updated_at=2026-08-10T10:55:50Z`, 49%); the canonical page remains 200/self-canonical/indexable but has no SSR-visible live intelligence, so `live` remains ineligible for the SSR contract. The future DW/GR upcoming row still has no exact opening-model row, and the completed BB/BL sample has no usable SSR intelligence. Both scraper candidate endpoints returned HTTP 504, so candidate stability remains open. This recovery is production feed evidence only; no GSC discovery, crawling, indexing, or ranking claim follows, and no production mutation was made.
- [-] **R1.2** Define the canonical SSR intelligence payload for the cohort: probability, updated timestamp, what changed, why, what matters next, lifecycle-specific fallback.
  Depends on: R1.1.  
  Implementation (2026-08-02): `CanonicalIntelligenceView` now carries an explicit SSR-visible `nextStep`; the canonical card renders it under `What matters next:`. Completed directs a reader to scorecard/probability-path review and related entities; live directs attention to the next scoring phase and update; upcoming directs attention to toss and confirmed XI. The existing reason remains the public-safe "what changed/why" field, with lifecycle-specific fallback text. App, server, and spec TypeScript checks pass; two stale spec fixtures were aligned with their current production APIs and the dedicated `test-intelligence` target now includes the lifecycle spec. The broad Karma run reaches Chrome but fails in unrelated legacy harness setup (`AddCustomerComponent` lacks Material test imports, and `projects/route/tsconfig.spec.json` is missing). The focused target now reaches Chrome and passes all 63 specs. This validates the local contract only: no production rollout is approved until a narrow frontend build and raw SSR recheck prove the field on a fresh eligible canonical page.
- [-] **R1.3** Render the lifecycle-appropriate intelligence block in canonical `/cric-live/{slug}` HTML without displacing score/state from the first viewport.  
  Depends on: R1.2.  
  Progress (2026-08-01): frontend commits `10b18ea` and `0bacca7`; production image `macubex/victoryline-frontend:20260801-canonical-intelligence-r2-0bacca7`. Raw normal and Googlebot SSR HTML prove the completed sample's final-answer module and one H1, with no stale `Upcoming 0/0` conflict.  
  Live/completed SSR proof (2026-08-01): current image `20260801-terminal-intelligence-r1` retains the compact answer for eligible live BP-W/WF-W and completed EDR/SDS. Googlebot HTML for EDR/SDS is 200/self-canonical/indexable/one-H1 with visible final `100%` probability and no stale `Upcoming 0/0`; the 390px browser shows the same completed answer after hydration. Remaining proof: an eligible upcoming lifecycle sample.
- [-] **R1.4** Verify hydration parity and stale/unavailable model fallbacks.  
  Depends on: R1.3.  
  Mobile production proof (2026-08-01): after restoring a valid dependency set, commit `3b604fd` and regression compatibility commit `a56d340` passed focused type-check plus local and Node-12 Docker SSR builds. Image `20260801-canonical-url-hydration-r1` was deployed and a 390px browser kept completed MSG/TR on its bare `/cric-live/{slug}` URL after Material selected the default Scorecard tab; its final intelligence module stayed visible. Subsequent image `20260801-terminal-intelligence-r1` preserves that guard and additionally proves the repaired EDR/SDS terminal model row at 390px: bare canonical URL, completed 100% answer, and no supporting-route navigation.  
  Remaining proof: an eligible upcoming sample once the shadow-only opening model is revised; do not fabricate an upcoming answer or treat its current unavailable state as parity success.

## R2 — Search intent and discovery

- [ ] **R2.1** Apply lifecycle title, H1, description, and visible-answer templates to the cohort only.  
  Depends on: R1.3.  
  Proof: each metadata promise maps to a visible SSR module on the same URL.
  GSC diagnostic (2026-08-03): test exact-match disambiguation, not broad-keyword stuffing. The 90-day leading demand `br vs sgr live score` has 62 clicks / 644 impressions / position 5.77 but is distributed between two legitimate recurring-fixture pages in different competitions. The approved experiment is team pair + lifecycle state + competition + match number in the existing lifecycle template and SSR answer. Keep generic `today` timing language on the schedule hub, which currently receives low-ranking 28-day impressions, and preserve one canonical match URL per fixture.
- [-] **R2.2** Verify SSR discovery links from live-score/schedule/series/entity surfaces into the cohort.  
  Depends on: R1.1.  
  Progress (2026-08-01): raw production HTML for `/live-score`, `/cricket-schedule/today`, and `/matches` contains contextual canonical hrefs for both controlled completed and upcoming URLs. Both URLs are also in `sitemap-matches-0001.xml`; the completed page is self-canonical and indexable.  
  Baseline correction (2026-08-09): the new standalone cohort monitor inspected `/`, `/matches`, `/live-score`, `/live-score/today`, and `/cricket-schedule/today` for nine real 12–48-hour upcoming URLs and found zero exact SSR links. Existing historical/live evidence therefore cannot be reused as current upcoming proof. The frontend now has a narrow SSR prematch feed-link lane in source; deploy it, then rerun the same cohort before marking this task complete.
  Rollout proof (2026-08-09): frontend image `macubex/victoryline-frontend:20260809-prematch-ssr-r1-63f00d1` (digest `sha256:8fa05cc80e25f36be067fbb48069f1678354d377b43c3b458f033d6efd5bb064`) is healthy in production. All nine sampled upcoming URLs appear in raw SSR HTML on `/live-score`, `/live-score/today`, and `/cricket-schedule/today`; the cohort is 8/9 technically ready (88.9%). The remaining Tezpur Titans/Jorhat Stallions page is blocked only because its page lacks a reliable venue/location for `SportsEvent`; do not fabricate one. Keep R2.2 open until the gate reaches at least 90%.
- [x] **R2.3** Keep Match Intelligence and other child routes noindex and out of sitemaps during the experiment.  
  Depends on: R2.1.  
  Production proof (2026-08-01): normal and desktop Googlebot raw HTML for `/match-intelligence/msg-vs-tr-14th-match-the-hundred-2026-men-match-updates-ZKN` both return `noindex,follow`. The child route is absent from `sitemap-matches-0001.xml`; the matching canonical `/cric-live/{slug}` URL is present. The child keeps a self canonical because it is explicitly excluded from indexing, while the canonical match page remains the discovery owner.
- [x] **R2.4** Define hub separation: `/live-score` owns live scores; `/cricket-schedule/today` owns timing; neither makes unsupported prediction promises.  
  Depends on: R2.1.  
  Production proof (2026-08-01): `/live-score` renders `Live Cricket Matches & Scores Today` in title/H1; `/cricket-schedule/today` renders `Cricket Schedule Today`. Neither raw SSR body makes `win probability`, `match prediction`, or `who will win` promises. Prediction intent stays on eligible canonical match pages.
- [-] **R2.5** Add an auditable discovery ledger for every controlled match: sitemap shard/lastmod, SSR-link source, GSC coverage state, last crawl, and follow-up inspection date.  
  Progress (2026-08-09): StartupOS now persists canonical identity and scheduled start across runs, plus first sitemap/SSR sightings and explicit GSC observation history with latency fields. Remaining: feed URL Inspection observations at T−24h/T−6h/T−1h/T+24–72h into the ledger and verify the 90% outcome target.
  Indexing execution plan (2026-08-10): (1) keep each eligible canonical page in the 12–48-hour cohort; (2) verify 200/self-canonical/index,follow/one-H1/SportsEvent/sitemap/SSR-hub-link before any manual action; (3) inspect in GSC at T−24h, T−6h, T−1h, and T+24–72h; (4) use manual GSC **Request indexing** only for verified priority pages; (5) calculate first discovered, first crawl, first indexed, and T−1h margin from explicit GSC timestamps. The StartupOS monitor now emits `gsc-action-queue.json` with these deadlines and a no-API manual-request recommendation; it never calls the Google Indexing API.
  CREX benchmark (2026-08-10): the investigation report `docs/seo/crex-live-cricket-seo-discovery-investigation-2026-08-10.md` verifies a multi-path system—stable early match URLs, a large match sitemap, a real Google News sitemap, series/team/homepage links, and selected editorial surfaces. The report does **not** prove that News sitemap inclusion caused ranking, and Crickzen will not copy NewsArticle/LiveBlogPosting markup or create a News sitemap for ordinary SportsEvent pages. The highest-confidence Crickzen gap is crawl-anchor quality: sampled hub anchors can fall back to `TBD vs TBD match preview` even when the stable slug carries fixture identity. Spec 050 extends `buildCanonicalMatchLinkLabel` so blank fields and placeholder labels such as `TBD`, `Team 1`, and `Team 2` derive `HK vs TAN`-style identity from the canonical slug when available. Commit `3a6dc08` is deployed as frontend image `macubex/victoryline-frontend:20260810-crawl-anchor-r1-3a6dc08` (digest `sha256:2e03413932b1f26f0c9f9d3afcc9e5a9084063c80e0913c4982a27e49445f9cc`). Raw production SSR for `/live-score`, `/live-score/today`, and `/cricket-schedule/today` now returns 48 `/cric-live/` links per hub with `TBD vs TBD=0` and `Team 1|Team 2=0`; sample anchors include `HK vs TAN match preview`, `ITA vs UGN match preview`, `EDR vs WDL match preview`, and `AFG vs IRE match preview`. GSC T−24/T−12/T−1h outcome proof remains pending.
  Depends on: R1.1 and R2.2.  
  Progress (2026-08-01): the weekly report now has baseline ledger rows for the completed and upcoming controlled URLs. Both are in `sitemap-matches-0001.xml` and linked from `/live-score` SSR, but GSC reports `URL is unknown to Google`; no manual URL submission follows from this.  
  Live inspection (2026-08-02T17:04 IST): GSC URL Inspection for current live SRL-W/TR-W reports **Discovered – currently not indexed**; both `sitemap.xml` and `sitemap-matches-0001.xml` are detected, but Google has no crawl, canonical, or referring-page record yet. The read-only live test reports **URL is available to Google**, with one valid Breadcrumb item and one valid Event item. A single normal GSC **Request indexing** action was submitted for this current eligible canonical page; GSC confirms only addition to its priority crawl queue, not indexing or ranking.  
  Remaining proof: add a fresh live row, actual GSC first-crawl/index details, and follow-up inspections before broad rollout.
  Heartbeat evidence (2026-08-09T17:28 IST): the exact live public-model row for `nds-vs-ndt-18th-match-delhi-premier-t20-league-2026-match-updates-13BM` is fresh (`updated_at=2026-08-09T11:57:55.309622`, T20 all-gender v2, 100% probability, 24 history points). Readiness remains `live=eligible`; this is model/canonical readiness evidence only, not GSC discovery or indexing proof. The controlled upcoming candidate still has no exact opening-model row, so the cohort remains blocked from SSR intelligence rollout.
  Post-rollout cohort ledger (2026-08-09T18:02 IST): the StartupOS monitor sampled 9 of 33 upcoming rows; every sampled URL is in a sitemap and has three exact SSR hub links. Eight pages pass the complete technical contract. The ninth (`js-vs-tt-19th-match-assam-premier-league-2026-match-updates-13AE`) is 200/self-canonical/indexable with match-specific metadata but has no `SportsEvent` because no reliable venue/location is available. Technical rate is therefore 88.9% (below the 90% target); GSC discovered/indexed remain unknown with zero explicit observations.
  Heartbeat evidence (2026-08-09T18:08 IST): the exact live public-model row for `nds-vs-ndt-18th-match-delhi-premier-t20-league-2026-match-updates-13BM` remains fresh (`updated_at=2026-08-09T12:38:48.984058`, T20 all-gender v2, 100% probability, 24 history points). Readiness remains `live=eligible`; this is model/canonical evidence only, not GSC discovery or indexing proof. The readiness guard still selects an upcoming row with no exact public-model row, so opening intelligence remains blocked independently of prematch link discovery.
  Heartbeat evidence (2026-08-09T18:39 IST): production returned 33 upcoming, 5 live, and 2,056 completed catalogue rows. The public prediction feed returned 11 rows; the exact NDS/NDT source URL resolves to a fresh running T20 all-gender v2 row (`updated_at=2026-08-09T13:09:52.068831`, 100%, 24 history points), so `live=eligible` remains true. The guard-selected upcoming DBS-W/LEI-W ODI URL still has no exact public opening-model row. Scraper `/prediction-candidates` and `/prematch-candidates` both returned HTTP 504 during this check; no production mutation was made because the live handoff is healthy and the upcoming opening-row gate is unchanged. This is model/catalogue evidence only, not GSC discovery or indexing proof.
  Heartbeat evidence (2026-08-09T19:09 IST): production still returns 33 upcoming, 5 live, and 2,056 completed rows; the public feed currently contains 4 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T13:39:28.506166`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Upcoming DBS-W/LEI-W still has no exact public opening-model row, and both scraper candidate endpoints again return HTTP 504. No safe production intervention was justified; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T19:39 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed contains 10 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T14:08:59.442080`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. The selected upcoming DBS-W/LEI-W row still has no exact opening-model row, and `/prediction-candidates` plus `/prematch-candidates` again return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T20:09 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed contains 10 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T14:39:04.765772`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Completed rotation now selects eligible MIL/TR (`mil-vs-tr-25th-match-the-hundred-2026-men-match-updates-ZL3`). Upcoming DBS-W/LEI-W still has no exact opening-model row, and both scraper candidate endpoints return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T20:39 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed is rotating between 4–6 rows during the check. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T15:08:56.589653`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Completed selection has rotated back to eligible BB/BL. Upcoming DBS-W/LEI-W still has no exact opening-model row, and both scraper candidate endpoints return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T21:09 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed contains 8 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T15:39:06.186683`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Completed selection is eligible MIL/TR. Upcoming DBS-W/LEI-W still has no exact opening-model row, and both scraper candidate endpoints return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T21:39 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed contains 8 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T16:09:16.441407`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Completed selection is eligible BB/BL. Upcoming DBS-W/LEI-W still has no exact opening-model row, and both scraper candidate endpoints return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Heartbeat evidence (2026-08-09T22:09 IST): production remains at 33 upcoming, 5 live, and 2,056 completed rows; the public feed contains 9 rows. NDS/NDT remains an exact fresh running row (`updated_at=2026-08-09T16:39:07.973423`, interpreted as provider UTC by the guard, 100%, T20 all-gender v2, 24 history points), so `live=eligible` remains true. Completed selection is eligible BB/BL. Upcoming DBS-W/LEI-W still has no exact opening-model row, and both scraper candidate endpoints return HTTP 504. No production mutation was made; GSC discovery/indexing remains unobserved.
  Live selection repair (2026-08-09T22:40 IST): the live feed rotated and the prior first-supported NDS/NDT row had no current exact public row, causing a false `live` freshness failure while SRL-W/WF-W had a fresh exact row. The readiness guard now prefers a supported live catalogue row with exact source URL, non-null probability, and timestamp within the five-minute gate before falling back to first-supported. Re-run selects `srl-w-vs-wf-w-27th-match-the-hundred-2026-women-match-updates-ZLA` and marks `live=eligible`; its public row is fresh (`updated_at=2026-08-09T17:10:55.296889`, The Hundred all-gender v1, 100%, 24 history points). The same sweep observed `/prediction-candidates` at HTTP 200 and `/prematch-candidates` at HTTP 504; a direct candidate follow-up was transiently proxy-failed, so candidate stability remains open. No production mutation was made.
- [ ] **R2.6** Replace the false "indexed today" operating signal with separate counters for sitemap inclusion, eligible discovery candidates, notification submitted (if ever eligible), GSC discovered, crawled, and indexed.  
  Depends on: R0.6.  
  Proof: production status and report labels cannot imply Google indexing from a successful API call.

## R3 — Query-to-intelligence measurement

- [x] **R3.1** Implement `match_view` with canonical URL, slug, lifecycle, surface, anonymous session ID, referrer/UTM, and source attribution where available.  
  Depends on: R1.3.  
  Deployment (2026-08-01): frontend commits `2926471` and `ec6de20`, deployed as `macubex/victoryline-frontend:20260801-canonical-match-view-r2-ec6de20` (digest `sha256:c9179f320d9f5282eac173e3aa5537a461d797220cd4969a91b01906257785dc`), add one browser-only canonical `match_view` per match route. The event carries `match_slug`, `match_path`, `lifecycle`, `surface`, anonymous session ID, landing path, referrer host, source attribution, and UTM fields. `gtag` is preferred and `dataLayer` is now a fallback so one interaction is not double-counted. The dependency-complete Docker SSR build passed; local Karma could not start because this checkout lacks the Angular CLI executable.  
  Production artifact proof: the served lazy bundle includes the event and session contract; normal, desktop Googlebot, and mobile Googlebot canonical-page probes remain 200/self-canonical/`index,follow`/one-H1 with the completed answer and valid `SportsEvent.startDate`/`location`.  
  Receipt proof (2026-08-02T11:15Z): the initial SSR-hydrated route did not emit because its encoded TransferState wrapper and lifecycle handoff were incomplete. Frontend commits `e7d8b49`, `f1b9b96`, `60d985e`, and `575c46a` repair that path without guessing lifecycle; production image `macubex/victoryline-frontend:20260802-match-view-hydration-r4-HEAD` is healthy. A real UTM-tagged SRL-W/TR-W canonical browser visit logged one deployed-bundle `match_view`; GA4 Realtime for `portfolio-digital-blazer` received `match_view = 1` alongside the canonical page view. This proves destination receipt, not Google indexing or ranking.
- [ ] **R3.2** Validate existing intelligence events with the same attribution contract.  
  Depends on: R3.1.  
  Proof: `prediction_view`, `prediction_interaction`, and `explanation_expand` validated in the destination.
  Implementation checkpoint (2026-08-02): frontend commit `db4b96d` makes `trackIntelligenceEvent` merge the same browser session, landing path, referrer/source, and UTM fields used by `match_view`, while preserving explicit lifecycle/surface fields. The TypeScript app check passes. The final hydrated analytics release is healthy. GA4 Realtime now records `prediction_view` and `intelligence_cta_impression` on the real SRL-W/TR-W canonical page; `prediction_interaction` and `explanation_expand` still need their own destination receipts, so R3.2 remains open.
  Browser-dispatch proof (2026-08-02T10:27Z): a real production browser navigation to the canonical SRL-W/TR-W URL with UTM parameters logged deployed-bundle `prediction_view` and `intelligence_cta_impression` dispatches. This proves the browser-side bridge executes on the canonical surface; it is not a GA4 DebugView, destination receipt, or engagement result.
  Interaction checkpoint (2026-08-02T11:30Z): frontend commit `a466c3a`, deployed as `macubex/victoryline-frontend:20260802-intelligence-events-r1-a466c3a`, wires the visible `Open prediction update log` disclosure to the existing one-time explanation/module interaction handlers. A real public intelligence-page expansion logged both `explanation_expand` and `prediction_interaction`; GA4 Realtime received `explanation_expand = 1`. `prediction_interaction` has not yet appeared in the Realtime event table, so R3.2 remains open pending its destination receipt.
- [ ] **R3.3** Implement relationship and return outcomes: `relationship_join` and `repeat_match_visit`.  
  Depends on: owned-channel decision.  
  Proof: completed join and later qualified session link back to an originating match/surface.
- [ ] **R3.4** Populate the weekly GSC query/page → event funnel report.  
  Depends on: R3.1–R3.2.  
  Proof: first report has a dated baseline and cohort rows.
  GSC baseline (2026-08-02): the current three-month Web report (last update 3.5 hours ago) shows 262 clicks, 4,502 impressions, 5.8% CTR, and average position 12.8 across the property. The leading query is `br vs sgr live score` (62 clicks / 644 impressions), confirming that exact live-score intent can earn traffic. This property-level baseline does not yet contain the new SRL-W/TR-W page or prove the query-to-event funnel; retain it as the pre-outcome benchmark.

## R4 — Historic match-page quality

- [x] **R4.1** Sample historic indexed match URLs and classify each as retain, improve, noindex, or consolidate.  
  Depends on: R2.4.  
  Inventory (2026-08-01): three GSC-indexed canonical samples — `dbs-vs-ess-33rd-match-t20-blast-2026-match-updates-ZUV` (the report has 1 click, 17 impressions, 7.1 average position), `ban-vs-ire-1st-t20-ireland-tour-of-bangladesh-2025-match-updates-0S8`, and `bh-w-vs-syt-w-8th-match-womens-big-bash-league-2025-match-updates-WQ6` — return indexable canonical HTML but visibly present stale `Upcoming 0/0` state and have no SSR-visible series or team link. All are classified **improve**, not noindex: the first has evidenced demand and the archive problem should be repaired at its authoritative lifecycle boundary before any exclusion decision.
- [x] **R4.2** Implement and test the approved weak-record policy.  
  Depends on: R4.1.  
  Production checkpoint (2026-08-01): commit `71b4302` is deployed as backend `macubex/victoryline-backend:20260801-historic-lifecycle-r1` (digest `sha256:ba7601bc0a839c177fe773d2d97ceabeb68390322160f6ea070f26fa3b8c2f00`) and frontend `macubex/victoryline-frontend:20260801-historic-terminal-r1` (digest `sha256:f41fe65750e4a0bd10a1685eff89d43a91470a5548191a617272882beb324eb5`). The targeted Java 17 regression test `CricketDataControllerTerminalLifecycleTest` passes. Both normal and Googlebot raw HTML for DBS/ESS are `200`, self-canonical, `index,follow`, and one-H1; they visibly show `COMPLETED` and the final ESS result with no `0/0 (0.0 ov)` shell.
  Follow-up: commit `2620b14` corrects the terminal summary's ambiguous legacy date label (previously parsed as 2001). It is now bundled into the live frontend r4 image, but DBS/ESS date-label proof was deferred while the live-cohort window took priority. Recheck normal + Googlebot raw HTML preserves the source date label, terminal result, no `0/0`, canonical, indexability, and one H1. Keep the Bangladesh/Ireland and Brisbane Heat/Sydney Thunder samples as improvement backlog until their catalogue lifecycle can be confirmed.
  Recheck complete (2026-08-01): normal and Googlebot raw HTML both return `200`, self-canonical, `index,follow`, and one H1 for DBS/ESS. The visible terminal state is `COMPLETED`, ESS `179/5 (19.4)` with no `0/0 (0.0 ov)` shell, and the source label is `Thu, May 31, 8:30 PM` rather than the former fabricated 2001 date. This confirms the deployed label repair for this sample; it does not yet prove retained team/series links (R4.3).
- [x] **R4.3** Ensure retained completed matches link to teams, series, and next relevant match.
  Depends on: R4.2.
  Local implementation (2026-08-02): canonical related navigation retains the existing real series hub and lifecycle-relevant links and now emits team-profile anchors only for authoritative player-stats or non-synthetic match-team IDs. Synthetic `match-team1`, `unknown-*`, and equivalent fallback IDs are explicitly suppressed so retained pages never advertise invented `/teams/` routes. The first production r4 probe proved the next-step field but exposed the remaining retained-record gap: LS/SB has no per-match player-stats snapshot, so it fell back to generic `/series` with no team URLs. The follow-up resolver accepts only one exact series-directory name match, then resolves team IDs only from that series' own standings and only when the completed match's team codes match; it cannot use globally ambiguous abbreviations. App/spec TypeScript checks and focused Chrome target pass (65 specs).
  Verified production (2026-08-02): the controlled server-side resolver accepts only the exact match-info series name (or an explicit `-men-`/`-women-` canonical-slug qualifier) and only team rows from that series' standings. It is deployed as `macubex/victoryline-frontend:20260802-retained-entity-stamped-r13` (digest `sha256:2a1fc879e9e4817ae8a3129bf4727569b01d47c33dcc9620b233fe83a52f2fc9`). Normal and Googlebot raw HTML for retained LS/SB are `200`, self-canonical, one-H1, retain the 100% completed probability, and visibly contain the exact series route plus London Spirit and Southern Brave team routes. Each route returns `200`. This proves retained completed entity-link coverage; it does not prove Google indexing/ranking.

  Metadata-correctness follow-up (2026-08-03, production verified): GSC-winning historical match pages exposed feed placeholders such as `TEAM 1 vs TEAM 2` and `No match name` in their title/H1/description. Frontend commit `ea188c5` rejects those placeholders and falls back to the canonical slug's real team and series context; the subsequent card/data-layer repair `5e91ab6` prevents placeholder names entering match cards and deduplicates the merged catalogue. The combined revision passes app/spec TypeScript checks and is deployed in frontend image `macubex/victoryline-frontend:20260803-placeholder-identity-r2-5e91ab6` (digest `sha256:1f7b40112b837c1b780a9826395b6524ab0b8bddb1813412d3d260ac700fcc41`). Normal and Googlebot SSR for the GSC-winning BR/SGR National T20 URL now both show `BR vs SGR Match Result & Full Scorecard`, one matching H1, and `Afghanistan National T20 Cup 2026` in the description, with no placeholder text in those crawler-visible fields. This is content-quality proof, not a Google indexing/ranking result.

## R5 — Learning and expansion decision

- [ ] **R5.1** Wait for and export the first 28-day post-rollout India GSC cohort.  
  Depends on: R2 and R3 release date.  
  Proof: query+page comparison with baseline date range.
- [ ] **R5.2** Decide per P1 cluster: improve, expand, consolidate, or stop.  
  Depends on: R5.1.  
  Proof: decision and evidence added to the weekly report and wiki.
- [ ] **R5.3** Consider an indexable reusable prediction hub only if canonical-page demand and engagement prove a separate repeatable user job.  
  Depends on: R5.2.  
  Proof: written value gate, non-duplicate page contract, and approval before implementation.

## Stop conditions

- Stop a proposed page if it has no distinct user job, no unique first-party explanation, or no owned next step.
- Stop rollout for a lifecycle if identity, freshness, or SSR parity fails.
- Do not claim ranking success from crawlability, a submitted sitemap, or a URL inspection pass alone.
- Do not deploy broad catalogue changes until the controlled cohort passes its evidence gates.

Heartbeat evidence (2026-08-10T13:54Z): the guard-selected upcoming candidate rotated to `sb-w-vs-tr-w-29th-match-the-hundred-2026-women-match-updates-ZLC`. An exact public row exists, but `updated_at`, probability, and prediction history are null/empty, so the opening artifact is unusable and fails freshness. The live NDS/NDT row is fresh (`updated_at=2026-08-10T13:53:26.831618Z`, 49%), but its canonical SSR still lacks the visible live-intelligence/win-probability block. Completed BRN/ITA has no exact public-model row and no SSR intelligence. This is a model/SSR lifecycle gate change only; GSC evidence remains 6/6 discovered and 0/6 indexed, and no indexing/ranking claim is made.

Heartbeat evidence (2026-08-10T14:22Z): the production catalogue endpoints were reachable with 16 upcoming, 5 live, and 2,056 completed rows, but the public prediction feed timed out after 15 seconds with no response. Both scraper `/prediction-candidates` and `/prematch-candidates` timed out after 12 seconds, and the NDS/NDT canonical page also timed out after 15 seconds. The readiness script therefore could not produce a fresh model/SSR assertion for this run; no lifecycle mutation was made. This is a production feed/page availability gate, not evidence of Google discovery, crawling, indexing, or ranking.

Heartbeat evidence (2026-08-10T14:52Z): the public prediction feed recovered with 9 rows and the readiness script completed. The selected upcoming SB/TR men’s Hundred URL has no exact public-model row; the live NDS/NDT row is exact and fresh (`updated_at=2026-08-10T14:54:11.090483Z`, 49%, one history point) but its canonical SSR still lacks visible live intelligence/win probability; completed BRN/ITA has no exact public row and no SSR intelligence. Both `/scraper/prediction-candidates` and `/scraper/prematch-candidates` returned HTTP 504. No production mutation was made; this is model/SSR/candidate evidence only, not GSC discovery, indexing, or ranking evidence.

Heartbeat evidence (2026-08-10T15:22Z): the readiness script failed again because the public prediction feed timed out after 30 seconds. Catalogue endpoints remained reachable with 16 upcoming, 5 live, and 2,056 completed rows; both `/scraper/prediction-candidates` and `/scraper/prematch-candidates` also timed out after 15 seconds. No fresh model or SSR assertion is made from this run, and no production mutation was made. This is a production availability gate, not evidence of Google discovery, crawling, indexing, or ranking.

Heartbeat evidence (2026-08-10T15:52Z): the readiness script again timed out at the public prediction feed, and a parallel sweep now found all six production endpoints unavailable within 10 seconds: upcoming, live, completed, public predictions, `/scraper/prediction-candidates`, and `/scraper/prematch-candidates`. No catalogue/model/SSR state can be asserted from this run and no production mutation was made. This is an availability incident, not evidence of Google discovery, crawling, indexing, or ranking.

Heartbeat evidence (2026-08-10T16:52Z): the upcoming, live, and completed catalogues recovered (16, 5, and 2,056 rows), while the public prediction feed still timed out and both scraper candidate feeds returned HTTP 504. The readiness script therefore still cannot assert a fresh model/SSR cohort. No production mutation was made; this is a partial availability recovery, not GSC discovery, crawling, indexing, or ranking evidence.

Heartbeat evidence (2026-08-10T17:52Z): the public prediction feed recovered with 7 rows and the readiness guard completed. The selected upcoming HK/TAN URL has no exact public opening row. Live NDS/NDT is now fully eligible: exact fresh row (`updated_at=2026-08-10T17:53:03.518861Z`, 49%, one history point) and SSR-visible live intelligence/win probability. Completed BB/BL is also eligible through its retained resolver row (100% final probability, history present) with SSR intelligence. Both scraper candidate feeds still return HTTP 504. This is a lifecycle/model gate recovery only; GSC evidence remains separate and no indexing/ranking claim is made.

Heartbeat evidence (2026-08-10T23:33Z): the guard-selected upcoming candidate rotated to `cdk-vs-ndt-22nd-match-delhi-premier-t20-league-2026-match-updates-13BQ`; it still has no exact public opening-model row, so upcoming remains ineligible. Live NDS/NDT and completed BB/BL remain eligible with SSR intelligence. The public feed returned 4 rows, while both scraper candidate endpoints returned HTTP 504. This is a cohort/model rotation only; no production mutation or GSC indexing/ranking claim was made.

Heartbeat evidence (2026-08-11T07:56Z): the exact upcoming CDK/NDT row is now present in the public feed, but it has null `updated_at`, probability, and history, so the opening artifact is unusable/stale and fails the 24-hour gate; SSR intelligence is also absent. Live NDS/NDT remains eligible with a fresh 49% row, and completed selection rotated to eligible MIL/TR. The public feed returned 5 rows; both scraper candidate feeds remain HTTP 504. This is a model lifecycle gate change only; no GSC discovery/indexing/ranking claim follows.

Heartbeat evidence (2026-08-11T08:26Z): the selected upcoming candidate rotated to `cs-vs-gr-21st-match-assam-premier-league-2026-match-updates-13AG`; its exact public row has null timestamp, probability, and history, so the opening artifact remains unusable/stale and fails the 24-hour gate. Live NDS/NDT remains eligible with fresh 49% SSR intelligence. Completed selection rotated to BRN/ITA with no exact public-model row and no SSR intelligence. The public feed returned 6 rows; both scraper candidate feeds returned HTTP 504. No production mutation or GSC indexing/ranking claim was made.

Heartbeat evidence (2026-08-11T08:56Z): the selected upcoming candidate rotated to `dur-vs-wor-65th-match-england-domestic-one-day-cup-2026-match-updates-ZZT`; it has no exact public opening-model row, so upcoming remains ineligible. Live NDS/NDT remains eligible with fresh 49% SSR intelligence. Completed selection rotated back to eligible BB/BL through retained history and SSR intelligence. The public feed returned 4 rows; both scraper candidate feeds returned HTTP 504. No production mutation or GSC indexing/ranking claim was made.

Heartbeat evidence (2026-08-11T09:56Z): the readiness script failed at the public prediction feed after 30 seconds. A parallel sweep found upcoming/live/completed catalogues reachable with 6/5/2,056 rows, while public predictions and both scraper candidate feeds timed out within 12 seconds. No fresh model/SSR assertion is made from this run and no production mutation was made. This is a partial availability regression, not GSC discovery, crawling, indexing, or ranking evidence.

Heartbeat evidence (2026-08-11T10:26Z): the public feed recovered with 8 rows and the readiness guard completed, but the upcoming catalogue now has no valid future candidate: all 6 rows are still marked `UPCOMING` even though their scheduled starts (06:00, 08:00, 08:30, or 10:00 UTC) are already past the current 10:26 UTC check. The guard correctly returns `NO_SUPPORTED_CANDIDATE` instead of treating stale rows as pre-match. Live NDS/NDT and completed BB/BL remain eligible; both scraper candidate feeds and `/scraper/health` return HTTP 504. This identifies a narrow scraper/lifecycle handoff gap; no restart or production mutation was made without a verified safe target. No GSC discovery/indexing/ranking claim follows.

Heartbeat evidence (2026-08-11T14:21Z): the upcoming catalogue still contains 6 stale `UPCOMING` rows with no future scheduled start, so the guard remains `NO_SUPPORTED_CANDIDATE`. Live NDS/NDT remains eligible. Completed selection rotated to BRN/ITA, whose resolver returns 404/no exact public-model row, so completed is temporarily ineligible; this is a rolling retention-sample rotation, not a broad lifecycle regression. The public feed returned 9 rows; both scraper candidate feeds remain HTTP 504. No production mutation was made and no GSC discovery/indexing/ranking claim follows.

Production restart (2026-08-11 ~15:50–16:00Z): after the page-load report, the pinned backend, frontend, and scraper containers were restarted with `docker compose -f docker-compose.prod.yml restart backend frontend scraper`; no image rebuild, retag, or dirty-tree mutation was performed. The server tree was confirmed dirty beforehand and preserved. Post-restart `docker compose ps` showed all three services healthy. Public `/health`, the canonical NDS/NDT page, `/scraper/health`, `/scraper/prediction-candidates`, `/scraper/prematch-candidates`, and the public prediction feed returned 200 during warm-up; prematch returned 3 exact candidates, prediction returned 5, and the public feed later returned 4 live rows including a fresh NDS/NDT 49% row. The readiness guard then reported the page 200 but live SSR intelligence absent, with upcoming MSG/SRL and completed BRN/ITA lacking exact public rows. This resolves the immediate page-load/service-health incident but not the model-to-SSR handoff; no GSC indexing/ranking claim follows. `/api/ws/info` still returned 502 and should be treated as a separate websocket-path follow-up.

Post-restart recovery (2026-08-11T15:59Z): the readiness guard now marks live NDS/NDT fully eligible again—fresh exact 49% public row (`updated_at=2026-08-11T15:58:47Z`), canonical 200/self-canonical/indexable/one-H1, and SSR-visible live intelligence. The prematch feed is healthy with three exact upcoming candidates (DW/NR, CSG/SS, and BL/TT), but no opening-model row is published for the selected MSG/SRL catalogue sample; completed BRN/ITA still has no exact retained row. Scraper health is 200, while prediction-candidates remains intermittently slow. This is lifecycle/model/SSR recovery only; GSC discovery/indexing/ranking remains unverified.

Heartbeat evidence (2026-08-11T16:29Z): the public feed rotated to CAN/UAE as the live source; its exact row is fresh (`updated_at=2026-08-11T16:28:43Z`, 5%, 24 history points), but the canonical page currently lacks SSR-visible live intelligence, so live is ineligible. Completed rotation selected SCO/UAE; its retained row has 61% but only one history point, so it fails the completed retention gate and SSR intelligence is absent. Upcoming MSG/SRL still has no exact opening row. Scraper health, prediction-candidates, and prematch-candidates all returned 200. No production mutation was made; this is lifecycle/model/SSR rotation evidence only, not GSC indexing/ranking evidence.

Heartbeat evidence (2026-08-11T16:59Z): the guard-selected upcoming candidate rotated to BRN/SIN and has no exact public opening row; completed selection rotated to MSG-W/SRL-W and also has no exact retained row. Live CAN/UAE remains an exact fresh row (`updated_at=2026-08-11T16:58:40Z`, 9%, 24 history points), but canonical SSR still lacks visible live intelligence, so live remains ineligible. Scraper health is 200, prematch candidates are 200 with three rows, and prediction-candidates timed out. No production mutation or GSC indexing/ranking claim was made.

Heartbeat evidence (2026-08-11T17:29Z): the guard now selects live NDS/WDL and completed NOTS/SUR as eligible through exact retained resolver rows (both final 100%, with 69 and 12 history points respectively); their canonical SSR contracts include visible intelligence. Upcoming remains BRN/SIN with no exact opening row. The public rolling feed contains 3 other live rows, while scraper health, prediction-candidates, and prematch-candidates all return 200. This is a lifecycle rotation/recovery only; no GSC discovery/indexing/ranking claim follows.

Heartbeat evidence (2026-08-11T18:29Z): live rotated to MSG/SRL and is eligible with a fresh exact 45% row (`updated_at=2026-08-11T18:28:16Z`, 24 history points) and SSR intelligence. Completed rotated to GOA/OMA and is eligible through a retained 100% row with 55 history points and SSR intelligence. Upcoming BRN/SIN still has no exact opening row. Scraper health, prediction-candidates, and prematch-candidates all return 200; the public rolling feed contains 2 rows. No production mutation or GSC indexing/ranking claim was made.

Heartbeat evidence (2026-08-11T20:28Z): the live catalogue is currently empty, so the guard correctly returns `NO_SUPPORTED_CANDIDATE` for live rather than asserting a stale row. The public feed has one retained MSG/SRL row (100%, 24 history points), and completed selection now resolves to that eligible retained row. Upcoming has 39 catalogue rows but no exact opening row for the selected BRN/SIN sample. Public predictions and all scraper endpoints return 200. This is a normal live-window/rolling-catalogue transition, not GSC discovery/indexing/ranking evidence; no production mutation was made.

Heartbeat evidence (2026-08-12T07:35Z): the catalogue has rotated to upcoming DW/NR, live HK/UGN, and completed GOA/OMA. No exact public row is available for DW/NR or HK/UGN; the only public row is BT/JKM (0%, 24 history points), so the live selection is currently unsupported by the model feed. GOA/OMA retains an exact final row but its canonical SSR intelligence block is absent, leaving completed ineligible. Scraper health, prediction-candidates, and prematch-candidates all return 200. No safe production intervention was justified; this is lifecycle/model/SSR evidence only, not GSC discovery/indexing/ranking evidence.

Heartbeat evidence (2026-08-12T08:05Z): the first readiness attempt failed with HTTP 500 from the public prediction endpoint. A parallel sweep immediately afterward showed all catalogues, scraper health, prediction-candidates, and prematch-candidates at HTTP 200, with one unrelated BT/JKM public row and HK/UGN selected by scraper. A three-second retry completed and confirmed upcoming DW/NR has no exact opening row, live HK/UGN has no exact public row, and completed MSG/SRL lacks SSR intelligence despite retained model data. This was a transient public-feed error followed by a stable model/SSR gate; no production mutation or GSC claim was made.

Heartbeat evidence (2026-08-12T08:35Z): lifecycle rotation now selects upcoming AFG/IRE, live DW/NR, and completed GOA/OMA. The only public row remains unrelated BT/JKM (0%, 24 history points), so upcoming and live have no exact model rows. Completed GOA/OMA is eligible with retained model history and SSR intelligence. Scraper health, prediction-candidates, and prematch-candidates all return 200. No production mutation or GSC discovery/indexing/ranking claim was made.

Heartbeat evidence (2026-08-12T09:05Z): the public feed remains a single unrelated BT/JKM row (0%, 24 history points), while the guard selects upcoming AFG/IRE and live DW/NR with no exact model rows. Completed rotation selects MSG/SRL; retained model data exists but the canonical SSR intelligence block is absent, so completed is ineligible. Scraper health, prediction-candidates, and prematch-candidates remain 200. One initial public-feed request returned HTTP 500, but an immediate retry returned 200; no mutation was made and no GSC discovery/indexing/ranking claim follows.

Heartbeat evidence (2026-08-12T09:36Z): the guard-selected upcoming PAK-BL/PAK-GO and live DW/NR have no exact public model rows. Completed rotation returned to GOA/OMA with retained model data but no SSR intelligence, so completed remains ineligible. The public feed remains a single unrelated BT/JKM row (0%, 24 history points); scraper health, prediction-candidates, and prematch-candidates all return 200. No production mutation or GSC discovery/indexing/ranking claim was made.

Heartbeat evidence (2026-08-12T10:06Z): the guard-selected upcoming BL/TT and live AFG/IRE have no exact public model rows. Completed rotation selects MSG/SRL; retained data exists but SSR intelligence is absent, so completed is ineligible. The public feed remains only BT/JKM (0%, 24 history points). Scraper health, prediction-candidates, and prematch-candidates all return 200. No production mutation or GSC discovery/indexing/ranking claim was made.

Scraper recovery (2026-08-12T10:36Z): `/scraper/health` had regressed to HTTP 503 and the Docker scraper was `unhealthy`; candidate endpoints still returned 200. Following the production restart runbook, only the pinned `victoryline-scraper` container was restarted (no rebuild/retag or dirty-tree change). After restart `docker compose ps` reports scraper healthy; `/scraper/health`, `/scraper/prediction-candidates`, and `/scraper/prematch-candidates` return 200 with 5 live and 3 prematch candidates. The public model feed returned 200 on retry with its single BT/JKM row; the readiness attempt hit a transient HTTP 500 and was not used as proof. This resolves the scraper-health incident but not the model identity/SSR gates; no GSC indexing/ranking claim follows.

Dashboard prediction incident/recovery (2026-08-12T10:53–11:05Z): `crickzen-dashboard` was healthy-looking but its logs showed repeated `RuntimeError: can't start new thread`; an in-container exec also failed with `Cannot fork/procReady not received`, explaining blank/missing prediction panels. Only `docker restart crickzen-dashboard` was performed (no rebuild, retag, or dirty-tree mutation). After warm-up the pinned `machine_learning_bbl-dashboard:20260801-opening-r1` container is healthy, `/health` is 200, the scraper is healthy with 5 selected live candidates and 3 prematch candidates, and the public feed is stable with 4 current live rows: CSG/SS, W/WF, AFG/IRE, and DW/NR. The live catalogue has 5 rows, so PAK-BL/PAK-GO is still missing a model row. Its CREX URL contains `national-champions-cup`, which the current scheduler classifier does not map to `ODI Male`; no state file exists for that URL. The local model repo is dirty, so no image rebuild or classifier rollout was attempted. The current image differs from the known `20260801-predictor-cleanup-0425234` image, and the shared dashboard state directory contains 4,873 files; inspect/roll forward only through the dashboard rollout guard. Readiness now reports live AFG/IRE exact/fresh and canonical-ready but SSR-visible intelligence false; upcoming BL/TT and completed BRN/ITA lack exact rows. This is a production prediction/model handoff issue, not GSC discovery/indexing evidence.

SEO monitor execution repair and fresh cohort (2026-08-12T11:13Z): the Windows task was reporting `LastTaskResult=0` while the StartupOS runner returned `no_tasks`, because its selector only accepted `pending` tasks even though the recurring `seo-crickzen-prematch-cohort-001` task is intentionally `in_progress`. The runner now treats `pending` and `in_progress` as runnable and has focused tests (`11 passed`). A manual run now updates the durable cohort artifact instead of leaving it stale. The fresh 12–48-hour cohort contains 9 deduplicated canonical URLs; Test aliases are excluded. Technical readiness is 7/9 (77.8%), with two real pages blocked only because the production page omits `SportsEvent` when venue/location data is unavailable; sitemap membership and four SSR hub links are present. GSC observations are currently unknown for this new cohort, so discovery/indexing and T−1h outcomes remain pending. This fixes monitoring truthfulness and prevents Test aliases from entering the GSC queue; it does not claim Google indexing or ranking.

Direct GSC observation and controlled action (2026-08-12T11:18–11:22Z): the exact technical-pass ODW/WDL canonical `https://www.crickzen.com/cric-live/odw-vs-wdl-25th-match-delhi-premier-t20-league-2026-match-updates-13BT` was inspected in the connected `https://www.crickzen.com/` property. GSC reported `URL is not on Google` / `URL is unknown to Google`, no referring sitemap or page, and `Last crawl: N/A`. A manual Search Console **Request indexing** submission for this exact canonical was accepted into Google's priority crawl queue; no Indexing API request was made. The monitor was rerun with the observation and now reports GSC discovered 0/indexed 0 for a known denominator of 1 (technical cohort remains 7/9). An initial guessed `/cricket-live/...` path returned a live-test 404; this was corrected to the production `/cric-live/...` canonical and is not treated as a page failure. This is the first direct proof of the current discovery gap on a technically valid match page; indexing remains unverified until a later inspection observes a crawl/index outcome.

Heartbeat evidence (2026-08-12T11:36Z): `Assert-LiveMatchCohortReadiness.ps1` completed successfully. Live AFG/IRE is exact, fresh, lifecycle-ready, canonical-ready, and SSR-visible (`Eligible=True`). Completed MSG/SRL is retained-model/lifecycle/canonical-ready and eligible. Upcoming BL/TT has no exact public opening-model row and remains ineligible for prediction claims; this does not block URL discovery. The StartupOS cohort artifact reran at `2026-08-12T17:00:49+05:30`, still 9 URLs with technical readiness 7/9 and GSC known sample 0/1 discovered, 0/1 indexed; the T−1h gate remains pending. No new production mutation or Google Indexing API request was made.

Heartbeat evidence (2026-08-12T12:06Z): the readiness guard completed, but lifecycle/model state rotated. Upcoming BL/TT still has no exact opening row. Live AFG/IRE remains exact/fresh/lifecycle-ready and canonical 200, but the current SSR body no longer exposes visible win-probability intelligence (`HasSsrWinProbability=False`), so live is not eligible for the SSR gate. Completed rotated to BRN/SIN with no exact public retained row. A direct 200-page check confirmed the AFG/IRE canonical is reachable with one H1, but no visible win-probability phrase. This is a model-to-SSR handoff regression, not GSC evidence; no production mutation was made.

Heartbeat evidence (2026-08-12T18:29Z): the readiness guard completed successfully and the production feeds were all reachable (catalogues, public model feed, scraper health, prediction-candidates, and prematch-candidates all HTTP 200). Live rotated to BP/MIL and is exact/fresh/lifecycle-ready/canonical-ready/SSR-visible (`Eligible=True`); the public row reports 55% and `updated_at=2026-08-12T18:30:58.624802` with 23 history points. Upcoming rotated to ODW/WDL with no exact public opening row, so it remains ineligible for prediction claims but not blocked from URL discovery. Completed rotated to EDR/SDS and is eligible through retained model evidence. No production mutation or Google Indexing API request was made; GSC remains a separate timed-observation gate.

Heartbeat evidence (2026-08-12T19:29Z): all production catalogues, public model feed, scraper health, prediction-candidates, and prematch-candidates returned HTTP 200. Live BP/MIL remains exact/fresh/lifecycle-ready/canonical-ready/SSR-visible (`Eligible=True`). Upcoming ODW/WDL still has no exact opening row and remains a discovery candidate without prediction claims. Completed rotated to LS/WF and remains eligible through retained model evidence. No production mutation or Google Indexing API request was made; GSC timed evidence remains unchanged.

Experiment conclusion and decision (2026-08-13): the controlled work has answered the diagnosis, but not yet achieved the business KPI. The discovery architecture is present—stable `/cric-live/{slug}` canonicals, sitemap membership, real SSR hub links, 200/self-canonical/indexable pages, and recurring cohort measurement—but technical readiness is not reliable enough: the latest 9-URL cohort is 5/9 (55.6%), with four pages failing only because production lacks venue/location data needed for honest `SportsEvent` markup. The model-to-SSR pipeline also rotates and can temporarily remove visible live intelligence; this is a separate reliability gate and must not block pre-match URL publication. GSC outcome evidence is still sparse: the earlier cohort was 6/6 discovered and 0/6 indexed, while the direct ODW/WDL inspection was `URL unknown to Google`, no referring sitemap/page, and `Last crawl: N/A`; the manual request only entered Google's priority queue and did not prove crawling or indexing. Therefore the primary problem is not simply “submit the sitemap”: it is inconsistent page/data eligibility plus insufficient crawl/index outcome proof.

Scheduled monitor paused (2026-08-13): the Windows `Crickzen Prematch GSC Cohort Monitor` task was stopped and disabled at the user's request. Cohort artifacts and logs are preserved; re-enable the task before the next measurement cohort.

Requested-vs-indexed reconciliation (2026-08-13): the durable manual-request log contains seven current URLs. Six were requested in the 2026-08-10 18:05–18:13 IST batch: HK/TAN (`13GW`), ITA/UGN (`13GX`), CDK/NDT (`13BQ`), CS/GR (`13AG`), DUR/WOR (`ZZT`), and KT/WARKS (`ZZU`). Their recorded GSC inspection at 2026-08-10T11:04:11Z reported `Discovered – currently not indexed` for all six, with `Last crawl: N/A`; no indexed timestamp exists. ODW/WDL (`13BT`) was inspected at 2026-08-12T11:18:55Z and reported `URL is unknown to Google`, then manually requested at approximately 17:00 IST; no crawl or indexed timestamp exists. The tracker also contains an earlier SRL-W/TR-W request on 2026-08-02, likewise recorded as discovered-not-indexed with no crawl/index proof. Therefore, from the durable evidence, **none of the requested URLs has a recorded indexed state or indexed date**. The current Chrome automation could not reconnect because the ChatGPT extension is unavailable in the selected Chrome profile, so no fresh live GSC reinspection was claimed in this check.

Investigation focus shift (2026-08-13): the primary question is now **why Google does not advance valid match URLs from discovered/unknown to crawled and indexed**. The investigation must separate four gates: (1) URL discovery, (2) Google crawl/last-crawl receipt, (3) indexing selection, and (4) search visibility. For every fixed sample, collect GSC Page indexing reason, Last crawl, Google-selected canonical, rendered/live-test availability, sitemap/referring-page evidence, server access-log hits for Googlebot, response/body stability, content uniqueness versus sibling match pages, and visible pre-match value. The current evidence suggests two different failures: ODW/WDL is a discovery/knowledge failure (`URL unknown to Google`), while the six August 10 URLs were known but had no crawl (`Discovered – currently not indexed`). Do not treat a sitemap entry, manual request, 200 response, or structured data as proof of indexing. The next remediation should target crawl receipt and Google’s quality/selection decision, not more bulk submissions.

GSC Page indexing report supplied (2026-08-13): the property has 1,334 not-indexed URLs across the listed buckets. The largest bucket is `Discovered – currently not indexed` at 1,101 (82.5%), followed by `Crawled – currently not indexed` at 68 (5.1%), canonical/duplicate buckets at 112 combined (8.4%), and 53 combined 404/noindex/soft-404 URLs (4.0%). Robots blocking and redirects are zero. This changes the priority order: the dominant property-wide issue is discovery/crawl backlog, while the 68 crawled-not-indexed URLs represent a separate Google quality/selection problem. The 89 alternate-canonical and 23 Google-chosen-canonical rows require URL-family consolidation. These counts are property-wide, not yet proven to be all `/cric-live/` pages; the next GSC action is to export/sample URLs from each reason and classify the `/cric-live/` subset before implementing broad changes.

Events-report interpretation (2026-08-13): a high count of valid Event items means Google successfully parsed Event structured data on the pages it evaluated; it does **not** prove that every live-match URL was indexed, that the Event enhancement generated impressions, or that the pages ranked. A later impressions drop therefore cannot be attributed to invalid Event markup from this report alone. The current invalid count (6) is too small to explain a property-wide collapse by itself. The stronger hypotheses remain crawl/index backlog, canonical/duplicate selection, crawled-but-not-indexed quality decisions, lifecycle demand changes, or a deployment that stopped emitting Event markup on the relevant `/cric-live/` pages. Validate by comparing the Events valid-item timeline with Performance impressions, Page indexing reasons, exact `/cric-live/` URLs, and deployment dates; treat Events as a supporting diagnostic, not the primary SEO KPI.

Events-versus-Performance reconciliation (2026-08-13): the supplied `Chart.csv` is Search Console Performance data, not the Events enhancement count. Performance peaked at 1,392 impressions on 2026-05-29 (45 clicks, average position 11.2); the 2026-05-23–29 window totals 3,363 impressions and 204 clicks. It then fell to 342 impressions on May 30, 256 on May 31, 14 on June 1, 5 on June 2, and 2 on June 3. Average position improved from 11.2 on May 29 to 5.7 on May 30 while impressions fell, so rank position alone cannot explain the cliff. The separate Events series peaked at 77 valid items on May 28 and still showed 49 on June 1, then reached zero on June 17–26. Valid Event markup therefore persisted after the Performance collapse; invalid Event markup was not the initiating cause. The later zero and only small July/August recovery are consistent with Google evaluating a much smaller replacement cohort after crawl/index/SSR disruption, with later schema regressions possibly suppressing recovery. Exact URL-level crawl, indexing, canonical, and raw-Googlebot evidence remains the proof gate for a single root cause.

Commit audit against the supplied charts (2026-08-13): there is no application commit in the repository on May 30 or May 31, so the first Performance cliff is not attributable to a same-day SEO deployment. The strongest concrete regressions are: (1) the May 18 Angular SSR migration (`9aecec2`) introduced an 8-second bare-app-shell fallback and server-side scorecard fan-out; the June 9 fix (`5e32c0d`) confirms this could make listing links intermittently invisible to crawlers; (2) the May 21–22 scraper/dashboard outage and five-match cap reduced live coverage and prediction freshness; (3) the May 23 scraper DOM bug corrupted one live team's identity (`PBKSPP`), an isolated match-identity defect; and (4) the June 18 rich SSR pass (`7ee613f`) derived `SportsEvent.startDate` from a narrow field and was followed by the June 19 hardening fix (`5898287`) after invalid/misdated Event output. The June 2–3 SEO commits occurred after the Performance collapse, so they cannot be its initiating cause. These commits show real crawl/data/schema debt and likely weakened recovery, but they do not prove one commit caused the May 30 cliff; the next proof remains URL-level GSC crawl/index and Googlebot-render evidence.

Decision: continue the experiment only as a bounded remediation run, not indefinitely. (1) Repair venue/location identity data and any remaining schema omissions until at least 90% of sampled upcoming pages pass the technical contract; never invent venue data. (2) Keep early canonical publication independent of opening-model availability, with a T−24/T−12/T−6/T−1 checkpoint ledger. (3) For the next three cohorts, inspect a small fixed sample in GSC at T−24/T−6/T−1/T+24–72 and manually request indexing for at most one or two technically valid priority URLs; do not use the Indexing API or bulk-submit. (4) If technical readiness reaches >=90% but discovery remains <90% at T−1h, strengthen the crawl graph (homepage, live-score, schedule, series/team links and fresh sitemap timestamps). If discovery reaches >=90% but indexing remains <90%, stop adding crawl-path mechanics and improve distinct match-page value/quality or reduce low-demand URL scope. (5) After three clean cohorts, make a go/no-go decision from observed GSC latency; do not claim “near-guaranteed indexing” until the T−1h indexed rate is actually >=90% across a meaningful sample.

Manual readiness check (2026-08-12T21:14:51Z heartbeat; scheduled Windows task was not run): `Assert-LiveMatchCohortReadiness.ps1` completed. Upcoming `odw-vs-wdl-25th-match-delhi-premier-t20-league-2026-match-updates-13BT` remains canonical-ready but has no exact public opening-model row and no SSR intelligence. Production live catalogue currently has no supported-format candidate, so no live model/SSR claim is made. Completed `bp-vs-mil-32nd-match-the-hundred-2026-men-match-updates-ZLK` remains eligible with exact fresh retained model evidence and SSR probability visible. Catalogue endpoints and scraper health/candidate endpoints returned HTTP 200; the direct guessed `/api/predictions/public` path returned 401 and is not the readiness script’s public model endpoint. No production mutation, GSC request, or scheduler run was made.

Spec 052 implementation checkpoint (2026-08-13): `specs/052-newsarticle-schema-eligibility/` formalizes the lifecycle-aware structured-data policy. Upcoming match pages remain `Article` plus `SportsEvent` only when trustworthy `startDate` and `location` exist; they never receive blanket `NewsArticle` or `LiveBlogPosting`. Genuine non-upcoming high-value coverage may emit `NewsArticle` plus `LiveBlogPosting` only when it has at least three substantive timestamped commentary updates and a real modification time; otherwise the page falls back to one generic `Article`. The existing SportsEvent guard remains independent and never fabricates venue/location values. `LiveMatchUpdate.source` now distinguishes `commentary` from `synthetic` summaries so generated score text cannot qualify as editorial evidence, and an explicit eligibility-reason helper records suppression reasons. Focused frontend TypeScript compilation (app and server) and the browser build passed with the repository Node/OpenSSL compatibility flag; the lifecycle assertions passed within the full Karma run (170 passed, 52 unrelated baseline Angular/provider/configuration failures remain). No production deployment, News sitemap, Google Indexing API request, or GSC indexing/ranking claim was made. Remaining gates are raw SSR/Googlebot parity, narrow frontend rollout, ledger schema/eligibility recording, and the three-cohort GSC T−24/T−6/T−1/T+24–72 measurement.
